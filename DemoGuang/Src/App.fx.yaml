App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =Set(
            varUser,
            User()
        );
        
        Set(VarSetSSN,false)
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

